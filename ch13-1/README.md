# 실습과제 1
- 배열명의 의미를 설명하라\
배열의 첫번째 원소의 주소 &arr[0]을 의미함.
- 배열 표현과 포인터(주소) 표현의 변환 공식을 설명하라\
  a[i] = *(a + i)\
  배열명을 포인터처럼 이용하여 주소와 값을 구하는 것이 가능하다.
- 배열의 첫 번째 요소의 주소만 알면 모든 원소의 주소를 계산할 수 있다. 방법을 설명하라.\
  임의의 배열명 arr이 있다고 할 때, 첫 번째 원소의 주소가 &arr[0]이므로 &arr[1], &arr[2] ... 인덕스 번호만 키우면 된다. 혹은 첫 번째 원소의 주소를 포인터 변수 ptr에 저장한다고 한다면, ptr + 0, ptr + 1, ptr +2 ...처럼 하면 된다.
- 배열의 첫번째 요소의 주소만 알면 모든 원소의 값을 계산할 수 있다. 방법을 설명하시오.\
  임의의 배열명 arr이 있다고 할 때, 첫 번째 원소의 값은 arr[0]이므로 arr[1], arr[2] ... 인덱스 번호만 키우면 된다. 혹은 첫 번째 원소의 주소를 포인터 변수 ptr에 저장한다고 한다면, *(ptr + 0), *(ptr + 1), *(ptr + 2) ... 처럼 간접참조연산자를 이용한다.
- 포인터에 대해 가능한 연산의 종류와 의미를 설명하라.\
간접참조 연산자(\*) : 포인터에 저장된 주소로부터 포인터가 가리키는 자료형의 크기만큼 읽거나 씀.
정수(n)가감 연산 : ±n×(포인터가 가리키는 자료형의 크기)만큼 주소 이동
증감 연산 : ±1×(포인터가 가리키는 자료형의 크기)만큼 주소 이동
- 포인터 연산을 기존의 산술연산과 다르게 정의한 이유는 무엇일까?\
  char*, int*, double*의 크기가 다 다르기 때문에 

---

# 실습과제 2 코드 설명
```c
#define _CRT_SECURE_NO_WARNINGS
```
VS scanf 보안 경고 방지용
```c
#include <stdio.h>
```
헤더파일 stdio.h을 포함하라
```c
int main(void)
```
메인함수 시작
```c
int grade[5];
```
5칸짜리 int형 배열 grade 선언
```c
int sum = 0, i, average;
```
int형 변수 sum, i 선언 후 sum은 0으로 초기화
```c
double average;
```
double형 변수 average 선언
```c
for (i = 0; i < 5; i++)
```
grade에 순서대로 값을 저장하기 위한 반복문
```c
		scanf("%d", (grade + i));
```
배열명 이용하여 포인터 표현으로 입력한 값을 저장
```c
for (i = 0; i < 5; i++)
```
grade에 저장된 값들을 순서대로 가져와 sum에 더하여 저장하기 위한 반복문
```c
		sum += *(grade + i);
```
배열명과 간접참조연산자를 이용해 sum에 더하여 저장
```c
average = sum / 5.0;
```
평균을 구함
```c
printf("성적 평균 = %lf\n", average);
```
평균 출력
```c
return 0;
```
0을 반환하고 main함수 정상 종료

---
# 실습과제 3
- 증감연산자 a++과 ++a의 차이를 설명하라
후위증감연산은 그 줄의 모든 연산을 끝낸 후 가장 마지막에 실행되어 다음줄로 넘어감.
전위증감연산은 그 줄의 모든 연산 이전에 가장 먼저 실행되고 나머지 연산이 우선 순위에 따라 진행됨.
- 주어진 코드의 문제점
배열명은 배열의 첫 번째 원소의 주소로 고정된 값이다. a의 주소값을 직접 바꾸는 증감연산은 할 수 없다.\

```c
#include <stdio.h>
```
헤더파일 stdio.h을 포함하라
```c
int main(void)
```
메인함수 시작
```c
int a[] = { 10, 20, 30 }, i, sum = 0;
```
int형 배열 a와 int형 변수 i, sum 선언 후 sum은 0으로 초기화
```c
int* p = a;
```
int* 형 포인터 p 선언후 배열명 a로 주소 저장
```c
for (i = 0; i < 3; i++)
```
포인터를 이용해 값을 저장하기 위한 반복문
```c
	sum += *p++;
```
포인터와 간접참조연산자와 후위증감연산자를 이용하여 sum에 저장된 값을 더해감
```c
printf("sum:%d\n", sum);
```
sum의 값을 출력
```c
return 0;
```
0을 반환하고 main함수 정상 종료

## 실행결과
<img width="244" height="70" alt="image" src="https://github.com/user-attachments/assets/41d48534-fb4c-4c01-bba6-3c46e8ae323f" />




